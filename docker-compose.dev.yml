version: '3'

services:
  db:
    environment:
      POSTGRES_USER: "myblog"
      POSTGRES_PASSWORD: "password"
    ports:
      - "5432:5432"
  web:
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - ./:/code
    ports:
      - "8000:8000"
